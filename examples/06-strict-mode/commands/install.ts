// Trivial handler — just prints. The point of the example is what happens
// at the parent level (typo suggestions, alias resolution), not what the
// command does once matched.
console.log('installed');
